<nav>
    <ul>
        <li>Liste de films</li>
        <li>Mon espace personnel</li>
        <a href={{ path('about_us') }}><li>A propos</li></a>
        {% if is_granted('ROLE_ADMIN') %}
            <a href={{ path('movies_create') }}><li>Créer un film</li></a>
        {% endif %}
    </ul>
</nav>
